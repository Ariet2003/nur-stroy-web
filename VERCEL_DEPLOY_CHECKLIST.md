# ✅ Чеклист для деплоя на Vercel

## 🚀 Изменения запушены!

Все исправления были успешно закоммичены и отправлены в репозиторий. Vercel автоматически начнет новый деплой.

## 📋 Что было исправлено:

### 1. ✅ Prisma Client для Vercel
- `package.json`: добавлен `postinstall` скрипт
- `prisma/schema.prisma`: добавлены `binaryTargets = ["native", "rhel-openssl-3.0.x"]`

### 2. ✅ Конфигурация API роутов для Next.js 16
- Удалена устаревшая конфигурация `export const config`
- Добавлены правильные экспорты для App Router:
  - `export const maxDuration = 60`
  - `export const dynamic = 'force-dynamic'`

### 3. ✅ Лимит размера body
- `next.config.ts`: добавлен `bodySizeLimit: '10mb'`

### 4. ✅ Сжатие изображений
- Автоматическое сжатие до 1920px
- Качество JPEG: 80%

## 🔍 Что нужно проверить в Vercel:

### 1. Переменные окружения
Убедитесь, что в настройках Vercel установлены все переменные:

```
DATABASE_URL=postgresql://...
IMGBB_API_KEY=your_key_here
JWT_SECRET=your_secret_here
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_password_here
```

**Важно:** Если вы добавили или изменили переменные окружения, нужно:
1. Перейти в Settings → Environment Variables
2. Проверить все переменные
3. Нажать "Redeploy" для применения изменений

### 2. Проверка деплоя

1. **Откройте панель Vercel:**
   - Перейдите на https://vercel.com/dashboard
   - Найдите проект `nur-stroy-web`
   - Откройте вкладку "Deployments"

2. **Дождитесь завершения деплоя:**
   - Статус должен стать "Ready" (зеленая галочка)
   - Если есть ошибки, кликните на деплой и проверьте логи

3. **Проверьте Build Logs:**
   - Найдите строку `✓ Prisma Client generated`
   - Убедитесь, что нет ошибок с Query Engine

### 3. Тестирование после деплоя

После успешного деплоя проверьте:

1. **Главная страница:**
   ```
   https://your-domain.vercel.app/
   ```
   - Должна загружаться без ошибок

2. **Админ-панель:**
   ```
   https://your-domain.vercel.app/admin
   ```
   - Войдите с учетными данными
   - Должен открыться дашборд

3. **Добавление портфолио:**
   - Кликните "Добавить работу"
   - Загрузите 2-3 изображения
   - Заполните название и описание
   - Нажмите "Добавить работу"
   - **Должно успешно сохраниться!**

4. **Проверка изображений:**
   - Откройте главную страницу
   - Прокрутите до раздела "Портфолио"
   - Изображения должны отображаться

## 🐛 Если ошибка все еще возникает:

### Проблема: Prisma Client not found

**Решение 1: Проверьте переменные окружения**
```bash
# В Vercel должна быть установлена DATABASE_URL
# Формат: postgresql://user:password@host:5432/database
```

**Решение 2: Принудительный redeploy**
1. В Vercel перейдите в Deployments
2. Кликните на последний деплой
3. Нажмите "..." → "Redeploy"
4. Выберите "Use existing Build Cache" → OFF
5. Нажмите "Redeploy"

**Решение 3: Проверьте логи сборки**
1. Откройте Build Logs
2. Найдите секцию "Running build command"
3. Проверьте, выполняется ли `prisma generate`
4. Должны быть строки:
   ```
   Environment variables loaded from .env
   Prisma schema loaded from prisma/schema.prisma
   ✔ Generated Prisma Client
   ```

### Проблема: 413 Payload Too Large

**Решение:**
- Изображения теперь автоматически сжимаются
- Попробуйте загрузить меньше изображений за раз (2-3)
- Убедитесь, что исходные изображения не огромные (>10MB)

### Проблема: ImgBB API ошибка

**Решение:**
1. Проверьте, что `IMGBB_API_KEY` установлен в Vercel
2. Проверьте лимиты на https://api.imgbb.com/
3. Убедитесь, что API ключ действителен

## 📞 Дополнительная помощь

Если проблемы остались:

1. **Проверьте Function Logs в Vercel:**
   - Deployments → [Ваш деплой] → Functions
   - Найдите `/api/portfolio`
   - Проверьте логи ошибок

2. **Проверьте Runtime Logs:**
   - Откройте Network tab в браузере (F12)
   - Попробуйте добавить работу
   - Посмотрите ответ от `/api/portfolio`
   - Скопируйте полный текст ошибки

3. **Проверьте базу данных:**
   - Убедитесь, что база данных доступна
   - Проверьте, что миграции применены
   - Попробуйте подключиться к БД через Prisma Studio

## ✨ Ожидаемый результат

После всех исправлений:
- ✅ Prisma Client успешно генерируется при деплое
- ✅ Query Engine для rhel-openssl-3.0.x найден
- ✅ Изображения загружаются на ImgBB
- ✅ Портфолио сохраняется в базу данных
- ✅ Работы отображаются на главной странице

**Удачи! 🚀**

