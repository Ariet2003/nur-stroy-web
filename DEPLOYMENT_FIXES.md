# Исправления для деплоя на Vercel

## ✅ Исправленные проблемы

### 1. Ошибка Prisma Client (rhel-openssl-3.0.x)

**Проблема:** `PrismaClientInitializationError: Prisma Client could not locate the Query Engine for runtime "rhel-openssl-3.0.x"`

**Решение:**
- ✅ Добавлен `postinstall` скрипт в `package.json`
- ✅ Настроены `binaryTargets` в `prisma/schema.prisma`
- ✅ Обновлен `build` скрипт для генерации Prisma Client

### 2. Ошибка 413 (Payload Too Large)

**Проблема:** Запросы с изображениями слишком большие для Vercel

**Решение:**
- ✅ Добавлена конфигурация `bodyParser` с лимитом 10MB в API роутах
- ✅ Реализовано автоматическое сжатие изображений перед отправкой
- ✅ Изображения ограничиваются максимальным размером 1920px
- ✅ Качество JPEG установлено на 80% для оптимального баланса

## 🚀 Что нужно сделать для деплоя

1. **Закоммитьте изменения:**
```bash
git add .
git commit -m "Fix Prisma and image upload issues for Vercel"
git push
```

2. **Настройте переменные окружения в Vercel:**
   - `DATABASE_URL` - PostgreSQL connection string
   - `IMGBB_API_KEY` - ImgBB API key
   - `JWT_SECRET` - Secret key for JWT
   - `ADMIN_USERNAME` - Admin username
   - `ADMIN_PASSWORD` - Admin password

3. **Пересоберите проект на Vercel**
   - Vercel автоматически запустит новый деплой после push
   - Или нажмите "Redeploy" в панели Vercel

## 📝 Технические детали

### Изменения в package.json
```json
{
  "scripts": {
    "build": "prisma generate && next build",
    "postinstall": "prisma generate"
  }
}
```

### Изменения в prisma/schema.prisma
```prisma
generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
}
```

### Изменения в API роутах
```typescript
export const config = {
  api: {
    bodyParser: {
      sizeLimit: '10mb',
    },
  },
};
```

### Сжатие изображений
- Максимальный размер: 1920px по большей стороне
- Формат: JPEG
- Качество: 80%
- Автоматическое сжатие при загрузке

## ⚠️ Важные замечания

1. **Лимиты Vercel:**
   - Hobby план: 4.5 MB максимальный размер ответа
   - Pro план: 4.5 MB максимальный размер ответа
   - Наше решение сжимает изображения до приемлемого размера

2. **База данных:**
   - Убедитесь, что база данных доступна из Vercel
   - Примените миграции после создания базы данных
   - Используйте connection pooling для production

3. **ImgBB API:**
   - Бесплатный план: 5000 запросов в месяц
   - Изображения хранятся на внешнем сервисе
   - Не забудьте добавить API ключ в переменные окружения

## 🔍 Проверка после деплоя

1. Откройте сайт и перейдите в `/admin`
2. Войдите с учетными данными администратора
3. Попробуйте добавить новую работу с изображениями
4. Проверьте, что изображения загружаются и отображаются корректно
5. Проверьте, что портфолио отображается на главной странице

## 🐛 Если проблемы остались

1. **Проверьте логи Vercel:**
   - Откройте панель Vercel
   - Перейдите в раздел "Deployments"
   - Кликните на последний деплой
   - Проверьте "Build Logs" и "Function Logs"

2. **Проверьте переменные окружения:**
   - Убедитесь, что все переменные заданы
   - Проверьте правильность значений
   - После изменения переменных нужен redeploy

3. **Проверьте базу данных:**
   - Убедитесь, что база данных доступна
   - Проверьте, что миграции применены
   - Проверьте connection string

4. **Проверьте ImgBB API:**
   - Убедитесь, что API ключ действителен
   - Проверьте лимиты использования
   - Попробуйте загрузить изображение вручную через API

## 📧 Поддержка

Если проблемы остались, проверьте:
- [Документация Vercel](https://vercel.com/docs)
- [Документация Prisma](https://www.prisma.io/docs)
- [Документация Next.js](https://nextjs.org/docs)

